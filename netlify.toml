[build]
  publish = "."
  command = "pnpm install --no-frozen-lockfile"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/patterns"
  to = "/.netlify/functions/patterns"
  status = 200
  force = true
